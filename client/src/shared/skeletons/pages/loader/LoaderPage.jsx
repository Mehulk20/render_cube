import { Loader } from '../../components/common';

/** Thin page wrapper so Loader sits alongside the other page-level skeletons in routing/preview. */
export default function LoaderPage() {
  return <Loader />;
}
